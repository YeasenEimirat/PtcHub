# PTC Hub v8 — نطاق المسؤولية حسب السنة

الفكرة بجملة: صار في نوعين مسؤولين — **أدمن عام** (كل السنوات)، و**مسؤول سنة**
(أدمن سنة أو مشرف سنة) بيشوف ويتحكّم بسنة واحدة فقط.

---

## 1) قاعدة البيانات

عمود واحد جديد على `Profiles`:

```
ScopeYear TINYINT NULL   -- 1..4 = مسؤول عن هذه السنة، NULL = كل السنوات
```

شغّل `MIGRATION-v8-ScopeYear.sql` مرة وحدة قبل ما تشغّل النسخة الجديدة.
الملف آمن للتكرار، وبيطبع لك جدول فيه مين مسؤول عن شو.

---

## 2) الباكند

| ملف | التغيير |
|---|---|
| `Models/UserScope.cs` | **جديد** — يمثّل "مين هذا المستخدم ومسؤول عن أي سنة" |
| `BLL/CourseCatalog.cs` | **جديد** — فهرس المساقات على السيرفر (كود ← سنة)، مولّد من `courses-index.js` |
| `BLL/Services/ScopeService.cs` | **جديد** — المرجع الوحيد لقرار الصلاحية: `Resolve` / `RequireStaff` / `RequireSuperAdmin` / `EnsureYearAllowed` |
| `AdminService` | قائمة الطلاب مفلترة بالسنة على مستوى الـ SQL؛ تغيير الصلاحيات صار للأدمن العام فقط، وبيقبل `ScopeYear` |
| `AnnouncementService` | السنة بتتقرّر من حساب المستخدم مش من الـ query؛ مسؤول السنة بينشر لسنته فقط وبيحذف إعلانات سنته فقط |
| `CourseFileService` | إضافة/حذف ملف بتتحقّق إنّ المساق من سنة المسؤول |
| `ProfileRepository` | `GetUserScope` جديدة، `UpdateRole` صارت تاخد `ScopeYear`، `CountAdmins` → `CountSuperAdmins` |
| `AnnouncementRepository` | خيار `generalOnly` للإعلانات العامة فقط |
| `Program.cs` | تسجيل `ScopeService` |

### قرار مهم: النطاق بينقرأ من القاعدة، مش من التوكن

التوكن عمره أيام. لو حطّيت النطاق جوّاه وغيّرت صلاحية مشرف اليوم، بيضلّ
شغّال بصلاحيته القديمة لحد ما ينتهي توكنه. فصار كل إجراء حسّاس بيسأل
القاعدة سؤال صغير واحد (`GetUserScope`) — تكلفته لا شي على ٨١ مستخدم،
ومكسبه إنّ التغيير بيسري فوراً.

### قرار مهم: فلترة الإعلانات انتقلت للسيرفر

قبل هيك الواجهة كانت تبعت `?year=2` والسيرفر يثق فيها. أي طالب بيفتح
الـ console وبيبعت `?year=3` وبيشوف إعلانات سنة تانية. هلأ الـ endpoint ما
عاد ياخد `year` أصلاً — بيقرأ سنة الطالب من حسابه.

نتيجة الفلترة الجديدة:

| مين | بيشوف |
|---|---|
| زائر بلا حساب | الإعلانات العامة فقط |
| طالب سنة ٢ | إعلانات سنة ٢ + العامة (النشِطة فقط) |
| طالب بلا سنة محدّدة بحسابه | العامة فقط |
| مسؤول سنة ٢ | إعلانات سنة ٢ + العامة (حتى المخفية) — بيحذف إعلانات سنته بس |
| أدمن عام | كل شي |

---

## 3) الواجهة

- **`auth.js`**: `scope_year` في البروفايل، `isSuperAdmin()` / `isScopedStaff()` / `scopeYear`،
  و`changeRoleByEmail(email, role, scopeYear)`.
- **`admin.html`**:
  - تبويب "الصلاحيات" بيظهر للأدمن العام فقط.
  - في نموذج الصلاحيات صار في خانة "السنة المسؤول عنها" (إجبارية للمشرف).
  - قوائم المساقات (الإضافة والتصفية) مفلترة على مساقات سنة المسؤول.
  - خانة سنة الإعلان مقفولة على سنته.
  - جدول الملفات والإحصائيات مفلترين على نطاقه.
  - جدول الطلاب بيبيّن نطاق كل مسؤول جنب رتبته.

> فلترة الواجهة للراحة بس. الحماية الحقيقية على السيرفر — جرّب تبعت
> `POST /api/files` بمساق سنة تانية من الـ console وبترجعلك 403.

---

## 4) القواعد المطبّقة

- المشرف **لازم** يكون مربوطاً بسنة (مشرف بلا سنة = مشرف على الكل، وهذا مش المقصود).
- الطالب ما إله نطاق إشراف أبداً (بينمسح تلقائياً).
- توزيع الصلاحيات للأدمن العام فقط — حتى ما يرفّع أدمن السنة حاله لأدمن عام.
- الأدمن ما بيغيّر صلاحية حاله.
- ممنوع يضلّ النظام بلا أدمن عام (ولا حتى بتحويل آخر واحد لأدمن سنة).

---

## 5) خطوات التشغيل

1. شغّل `MIGRATION-v8-ScopeYear.sql` على SSMS.
2. تأكّد من نتيجة الفحص إنّ حسابك أنت `Role='admin'` و `ScopeYear = NULL`.
3. `dotnet build` ثم `dotnet run`.
4. من لوحة التحكم → الصلاحيات: اكتب بريد الطالب، اختار "مشرف" أو "آدمن"،
   واختار "السنة الثانية فقط"، واحفظ.
5. المستخدم لازم يعمل **تسجيل خروج ودخول** (أو ريفرش) حتى تتحدّث واجهته.

---

## 6) تنبيه للصيانة

`BLL/CourseCatalog.cs` مولّد من `wwwroot/courses-index.js`. لو ضفت مساقاً
جديداً على الخطة، ضيفه بالملفين — وإلا مسؤول السنة رح يترفض عليه بـ 403
لأن السيرفر ما بيعرف الكود.
